## Implemented in Python.

### Let me explain what I HAVE done :) (again)

This code implements the Breadth-First Search (BFS) algorithm

We want to travel from point A to G, so i've simulated a graph using a `dictionary` in Python

In the video, we learned about the frontier, so I've created a `deque` to store unexplored nodes. I think this was the simplest way to demonstrate and test the algorithm.

the `visited` variable keeps track of visited nodes and prevents infinite loops.